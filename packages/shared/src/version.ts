// Copyright 2026 Nishant Bhandari
// SPDX-License-Identifier: Apache-2.0

/**
 * The package/product version. Surfaced in the generated OpenAPI document
 * (feature 4) so the API advertises a version, and used as a smoke-test export
 * so the coverage harness has something to run from the first commit.
 */
export const SWITCHBOARD_VERSION = '0.0.0';

/** Base path every REST route is mounted under (see CLAUDE.md API versioning). */
export const API_BASE_PATH = '/api/v1';
